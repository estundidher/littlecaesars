<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <dl>
      <dt>Order number:</dt>
      <dd><%= @order.code %></dd>
      <dt>Name:</dt>
      <dd><%= @order.customer.name %></dd>
      <dt>Email:</dt>
      <dd><%= @order.customer.email %></dd>
      <dt>Pick Up:</dt>
      <dd>
        <%= @order.pick_up.date.to_formatted_s :long %> <br>
        <%= @order.pick_up.place.address %> <br>
        <%= @order.pick_up.place.phone %>
      </dd>
      <dt>Pizzas</dt>
      <dd>
        <table class="table table-striped table-bordered">
          <thead>
            <tr>
              <th> Name </th>
              <th style="text-align: center; width: 10px;"> Qtd </th>
              <th style="text-align: right; width: 10px;"> Price </th>
            </tr>
          </thead>
          <tfoot>
            <tr>
              <th></th>
              <th style="text-align: center;"> Total </th>
              <th> <%= number_to_currency @order.price %> </th>
            </tr>
          </tfoot>
          <tbody>
            <% @order.items.each do |order_item| %>
              <tr>
                <td>
                  <% if order_item.instance_of?(CartItemSplittable) %>
                    <%= render 'orders/resume_detail', order_item:order_item.highest_half, label:'1/2', show_size:true %>
                    <br><br>
                    <%= render 'orders/resume_detail', order_item:order_item.lowest_half, label:'1/2', show_size:false %>
                  <% else %>
                    <%= render 'orders/resume_detail', order_item:order_item, label:nil, show_size:true %>
                  <% end %>
                </td>
                <td style="text-align: center;"> 1 </td>
                <td> <%= order_item.price %> </td>
              </tr>
            <% end %>

          </tbody>
        </table>
      </dd>
    </dl>
  </body>
</html>