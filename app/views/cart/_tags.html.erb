<span class="<%= 'topping' if type == 'additions' %>">
	<% if products != nil && products.any? %>
		<% products.each do |product| %>
			<%= render partial:'cart/tag', locals:{product:product, side:side, type:type, cart_item:cart_item} %>
		<% end %>
	<% end %>
</span>