<%= f.hidden_field :product_id, value: product.id %>

<div class="form-group <%= tb_has_error cart_item, :size %>">
  <%= f.label :quantity %>
  <%= f.select :quantity, (1..10), {}, {class: 'form-control'}  %>
</div>